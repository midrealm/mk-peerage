<h1><%=@group.titled_name%></h1>
<ul>
  <%= render 'draw_tree', node: @group %>
</ul>
<h3>Candidates</h3>
<ul>
<% @group.candidates.each do |candidate|%>
  <li><%= link_to candidate.sca_name, candidate_path(candidate.id) %></li>
<% end %>
</ul>
