<% node.children.each do |child|  %>
    <li><%= link_to child.titled_name, candidate_group_url(child.name) %>
    <% if child.children.any?  %>
      <ul><%= render 'draw_tree', node: child  %></ul>
    <% end  %>
   </li>
<% end %>
