<h1>Add New <%=peerage.capitalize%></h1>
<div class="container">
  <div class="col-md-4">
    <%= semantic_form_for peerage, url: controller.send("chambers_#{peerage}_admin_#{peerage}s_path") do |f| %>
      <%= f.inputs do %>
        <%= f.input :sca_name, label: 'SCA Name' %>
        <%= f.input :vigilant, as: :radio, label: 'Status', collection: [['Vigilant',true],['Elevated',false]] %>
        <%= f.input :email %>
      <% end %>
      <%= f.action :submit, as: :button,  label: "Create #{peerage.capitalize}", button_html: {class: 'btn btn-default'}, id: 'submit'%>
    <%end%> 
  </div>
</div>
