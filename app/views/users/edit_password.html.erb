<h2>Change your password </h2>

<%= bootstrap_form_for @user, url: users_edit_password_path do |f| %>

    <%= f.password_field :current_password, label: 'Current password'%>
    <%= f.password_field :password, label: 'New password'%>
    <%= f.password_field :password_confirmation, label: 'Confirm new password'%>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

